#include "BoolMatrix.h"
#include <iostream>
#include <cmath>
#include <time.h>
#include <stdlib.h>
#include <string.h>
#include <windows.h>
#include <stdio.h>
#include <iomanip>

using UI = unsigned int;
#define UC unsigned char


using std::cout;
using std::cin;



int main()
{
	BoolMatrix bm;
	char S[28];
	// cout << S;
	// // UI* M;
	// int m, n;
	// puts("Quantity of strings = ");
	// scanf_s("%d", &m);
	// puts("lenght of string = ");
	// scanf_s("%d", &n);
	// getchar();
	// M = new UI[m];
	// printf("enter %d strings of lenght %d:\n", m, n);
	// InputBM(M, m, n);
	// //puts("your Matrix:");
	// //OutputBM(M, m, n);
	// puts("Marked Matrix:");
	// Mark2(M, m, n);
	// delete[]M;
	// return 0;
}